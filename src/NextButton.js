//import { useState } from 'react';

const NextButton = ({disabled}) => {

    //const [disabledCheckVar, setDisabledCheckVar] = useState("disabled")

    return ( 
        <button disabled = {disabled}>Next</button>
     );
}
 
export default NextButton;